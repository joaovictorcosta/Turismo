/******************************************************************************/
/*            Generated by IBExpert 2009.03.25 28/11/2016 13:56:08            */
/******************************************************************************/

SET SQL DIALECT 3;

SET NAMES NONE;

SET CLIENTLIB 'C:\Program Files (x86)\Firebird\Firebird_2_1\bin\fbclient.dll';

CREATE DATABASE 'C:\Bases\TURISMO.FDB'
USER 'SYSDBA' PASSWORD 'masterkey'
PAGE_SIZE 16384
DEFAULT CHARACTER SET NONE;



/******************************************************************************/
/*                                   Tables                                   */
/******************************************************************************/

CREATE TABLE PAIS (
    CODIGO     INTEGER,
    DESCRICAO  VARCHAR(50)
);

CREATE TABLE ESTADO(
    CODIGO     INTEGER,
    DESCRICAO  VARCHAR(50),
    CODIGOPAIS INTEGER	
);

CREATE TABLE CIDADE (
    CODIGO     INTEGER,
    DESCRICAO  VARCHAR(50),
	CODIGOESTADO INTEGER
);

CREATE TABLE LOGRADOURO (
    CODIGO       INTEGER,
    DESCRICAO    VARCHAR(50),
	BAIRRO       VARCHAR(50),
	CODIGOCIDADE INTEGER
);

CREATE TABLE PARCEIRO (
    CODIGO  INTEGER,
	NOME    VARCHAR(100),
	STATUS  VARCHAR(1),
	TIPO    VARCHAR(50),
	CPFCGC  VARCHAR (14),
	DATANASCIMENTO DATE,	
	DATAULTVENDA DATE,
	DATACADASTRO DATE
);

CREATE TABLE PARCEIROENDERECO (
    CODIGOPARCEIRO  INTEGER,
	TIPO    VARCHAR(50),
	CODIGOLOGRADOURO INTEGER,
	NUMERO  VARCHAR (5),
	COMPLEMENTO VARCHAR(100)	
);

CREATE TABLE PARCEIROTELEFONE (
    CODIGOPARCEIRO  INTEGER,
	TIPO    VARCHAR(50),
	DDD     VARCHAR(2),
    TELEFONE VARCHAR(9)	
);

CREATE TABLE FORMAPGTO (
    CODIGO     INTEGER,
    DESCRICAO  VARCHAR(100),
    STATUS     VARCHAR(1),
    TIPO       VARCHAR(50)
);

CREATE TABLE PERMISSAO (
    CODIGOUSUARIO  INTEGER,
    CHAVE          VARCHAR(50),
    VALOR          VARCHAR(50)
);

CREATE TABLE PRODUTO (
    CODIGO        INTEGER,
    DESCRICAO     VARCHAR(100),
    STATUS        VARCHAR(1),
    TIPO          VARCHAR(50),
    SALDOINICIAL  INTEGER,
    DATACADASTRO  DATE
);

CREATE TABLE PRODUTOSALDO (
    CODIGOPRODUTO   INTEGER,
    CODIGOVENDA     INTEGER,
    STATUS          VARCHAR(1),
    SALDOUTILIZADO  FLOAT
);

CREATE TABLE RELATORIO (
    CODIGO     INTEGER,
    DESCRICAO  INTEGER,
    STATUS     VARCHAR(1),
    TELA       VARCHAR(25),
    CABECALHO  BLOB SUB_TYPE 0 SEGMENT SIZE 80,
    CONTEUDO   BLOB SUB_TYPE 0 SEGMENT SIZE 80,
    RODAPE     BLOB SUB_TYPE 0 SEGMENT SIZE 80,
    PORTA      VARCHAR(10),
    CAMINHO    VARCHAR(100),
    MARCA      VARCHAR(25),
    MODELO     VARCHAR(25)
);

CREATE TABLE TIPO (
    TABELA     VARCHAR(25),
    DESCRICAO  VARCHAR(100)
);

CREATE TABLE USUARIO (
    CODIGO        INTEGER,
    NOME          VARCHAR(50),
    SENHA         VARCHAR(50),
    DATAULTLOGIN  DATE
);

CREATE TABLE VENDA (
    CODIGO           INTEGER,
    CODIGOPARCEIRO   INTEGER,	
    CODIGOUSUARIO    INTEGER,
	CODIGOVENDEDOR   INTEGER,
	CODIGOGUIA	     INTEGER,
    STATUS           VARCHAR(1),
    DATACADASTRO     DATE,
    DATAVENDA        DATE,
    OBSERVACAO       BLOB SUB_TYPE 0 SEGMENT SIZE 80
);

CREATE TABLE VENDAITEM (
    CODIGOVENDA    INTEGER,
    CODIGOPRODUTO  INTEGER,
    STATUS         VARCHAR(1),
    QUANTIDADE     FLOAT,
    OBSERVACAO     BLOB SUB_TYPE 0 SEGMENT SIZE 80
);

CREATE TABLE VENDAPARCELA (
    CODIGOVENDA      INTEGER,
    CODIGOFORMAPGTO  INTEGER,
    STATUS           VARCHAR(1),
    VALOR            FLOAT,
    OBSERVACAO       BLOB SUB_TYPE 0 SEGMENT SIZE 80
);

CREATE TABLE ORDEM (
    TABELA VARCHAR(100),
	ORDEM INTEGER
);
